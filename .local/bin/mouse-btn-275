#!/bin/bash
# ~/.local/bin/mouse-btn-275

ACTIVE=$(hyprctl activewindow -j | jq -r '.class')

case "$ACTIVE" in
    "chrome-outlook.office.com__-Profile_1")
        xdotool key ctrl+shift+1
        ;;
    *)
        xdotool key alt+Left
        ;;
esac
